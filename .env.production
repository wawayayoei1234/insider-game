# Production config — used automatically by `next build` (NODE_ENV=production).
# Takes precedence over the local dev .env so a LAN IP can never ship to prod.
# All values are NEXT_PUBLIC_* (exposed client-side), so committing them is fine.
NEXT_PUBLIC_WS_URL=wss://api.insider-game.org/ws
NEXT_PUBLIC_AGORA_APP_ID=09d5f084d6894b6a97245676587730d5
